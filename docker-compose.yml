version: "3.7"
services:
  minecraft:
    build: .
    container_name: minecraft-server
    ports:
      - "9000:25565"
    volumes:
      - minecraft-data:/app
    restart: on-failure

volumes:
  minecraft-data: